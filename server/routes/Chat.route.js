const express = require("express");
const router = express.Router();

// ─── Chat Routes (Placeholder) ──────────────────────────────────
// TODO: Implement real-time chat routes
// Future routes:
//   POST   /api/chat/send       → Send a message
//   GET    /api/chat/:groupId   → Get messages for a group
//   POST   /api/chat/join       → Join a group chat
//   DELETE /api/chat/leave      → Leave a group chat
// ─────────────────────────────────────────────────────────────────

module.exports = router;
